# Runtime dependencies for the Carlos Council backend.
# Kept in sync with pyproject.toml. Used by the Dockerfile and local installs.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
openai>=1.50.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0
